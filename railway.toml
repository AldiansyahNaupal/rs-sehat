[build]
builder = "nixpacks"
watchPatterns = ["/*.php", "/resources/**", "/routes/**"]

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[plugins]
name = "phpmyadmin"